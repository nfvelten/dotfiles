#!/usr/bin/env bash
set -u

workspace="$1"

for _ in 1 2 3 4 5; do
    hyprctl eval "hl.dispatch(hl.dsp.workspace.toggle_special(\"$workspace\"))" >/dev/null 2>&1 && exit 0
    sleep 0.1
done

exit 1
